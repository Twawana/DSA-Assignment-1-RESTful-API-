[package]
org = "dsa612s_team"
name = "library_client"
version = "0.1.0"
distribution = "2201.10.0"

# If `bal build` complains about the distribution version above, run
# `bal version` and change the line above to match what's installed.
